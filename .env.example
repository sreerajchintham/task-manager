# Database
DATABASE_URL=sqlite:///./tasks.db

# App Settings
DEBUG=True
APP_NAME=Task Manageractivate

# Your terminal should now show (venv) at the beginning

# Install dependencies
pip install -r requirements.txt